import 'package:flutter/material.dart';

// Durações
const kSplashAnimationDuration = Duration(seconds: 2);
const kNavigationDelay = Duration(seconds: 1);

// Texto
const kSplashTitle = "Control Spider";

// Estilos visuais
const kSplashTextStyle = TextStyle(fontSize: 30, fontWeight: FontWeight.bold);
const kSplashSpacing = 20.0;

// Cores
const kSpinKitStartColor = Colors.cyan;
const kSpinKitEndColor = Colors.greenAccent;

// Circular Progress Styles
const kCircularProgressSize = 50.0;
const kCircularProgressStrokeWidth = 5.0;
const kCircularProgressGradientBegin = Alignment.topCenter;
const kCircularProgressGradientEnd = Alignment.bottomCenter;
